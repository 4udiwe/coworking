# Scheduler Service
Сервис планировщика, отвечающий за выполнение отложенных задач и генерацию событий по времени в системе коворкингов.

Основная задача — обеспечить надёжное выполнение time-based логики и централизованное управление фоновыми процессами.

## Data Flow

1. **Обработка события**

    Сервис получает событие из `booking.events`.

2. **Создание таймеров**

    Для нового бронирования создаются таймеры:
    - для уведомления о напоминании за **n** минут
    - для перехода бронирования в статус `completed`

3. **Работа таймеров**

    Фоновый worker контролирует время срабатывания таймеров.
    Когда таймер достигает установленного времени - публикует соответствующее событие в Kafka.

Подход с сохранением таймеров в БД и фоновым воркером гарантирует сохранность таймеров в системе. При падении сервиса или перезапуске таймеры не будут потеряны.

### Очистка сессий

Для периодической очистки завершенных сессий пользователей публикуется событие в топик `auth.events`, которое вызывает процедуру очистки старых сессий. Настройка очистки сессий производится в [config.yaml](config/config.yaml):
```
session_cleanup_worker:
  interval: 12h
  retention_days: 3
  topic: "auth.events"
```

## Интеграция

Сервис подписан на топик `booking.events`. Более подробно в [event-catalog](../docs/event_catalog.md)

## Конфигурация

Через `.env`. Для запуска можно скопировать `.env.example`